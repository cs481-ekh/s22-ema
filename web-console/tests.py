from _pytest.capture import capfd

from Schedule import *
from django.test import TestCase

#class TestCase(TestCase):

    #def test_Schedule_one(self):
        #schedule.every(3).seconds.do(background_job).tag('test')

        # Start the background thread
        #stop_run_continuously = run_continuously()

        #getback = str(schedule.get_jobs('test'))
        #print(getback)

        # Do some other things...
        #time.sleep(4)

        # Stop the background thread
        #stop_run_continuously.set()

        #assert getback == '[Every 3 seconds do background_job() (last run: [never], next run: 2022-02-18 16:26:07)]'



